# Soccer Planner

Minimal Expo Router app with four stack routes:

- Home
- Login
- Matches
- Match Details

## Run locally

Optionally set the API URL in `.env` (defaults to localhost for local development):

```bash
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000/api
```

```bash
npm install
npm run start
```
