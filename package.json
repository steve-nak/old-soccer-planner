{
  "name": "soccer-planner",
  "description": "Soccer Planner app: plan and organize pickup soccer matches",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=soccer-web\" \"npm run start --workspace=soccer-mobile\"",
    "build": "npm run build --workspace=soccer-web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "workspaces": [
    "soccer-web",
    "soccer-mobile"
  ]
}
